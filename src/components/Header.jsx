// write the component here and then export to App

import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <header>
        <nav>The nav goes here</nav>
      </header>
    );
  }
}

export default Header;
